class Solution:
    def nextGreaterElement(self, nums1, nums2):
        stack = []
        mp = {}

        for n in nums2:
            while stack and stack[-1] < n:
                mp[stack.pop()] = n
            stack.append(n)

        return [mp.get(x, -1) for x in nums1]
